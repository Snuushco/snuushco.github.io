"use client";
import { useState } from "react";
export default function InvoiceTool() { const [client, setClient] = useState("Voorbeeld Klant B.V."); const [desc, setDesc] = useState("Werkzaamheden juni"); const [amount, setAmount] = useState("500"); const vat = (Number(amount) || 0) * 0.21; return <main className="kassie-seo-page"><section className="kassie-seo-hero"><p className="eyebrow">Gratis tool</p><h1>Factuurgenerator</h1><p className="lead">Maak een eenvoudig factuurconcept zonder login. UBL/export en e-mailflow zijn de volgende bouwfase.</p></section><section className="kassie-tool"><label>Klant<input value={client} onChange={(e) => setClient(e.target.value)} /></label><label>Omschrijving<input value={desc} onChange={(e) => setDesc(e.target.value)} /></label><label>Bedrag excl. btw<input value={amount} onChange={(e) => setAmount(e.target.value)} inputMode="decimal" /></label><div className="invoice-preview"><h2>Factuurconcept</h2><p><strong>Aan:</strong> {client}</p><p><strong>Omschrijving:</strong> {desc}</p><p><strong>Excl. btw:</strong> €{(Number(amount)||0).toFixed(2)}</p><p><strong>BTW 21%:</strong> €{vat.toFixed(2)}</p><p><strong>Totaal:</strong> €{((Number(amount)||0)+vat).toFixed(2)}</p><button className="button" onClick={() => window.print()}>Print/download als PDF</button></div></section></main>; }
